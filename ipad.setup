#!/bin/sh

apk add git tmux neovim openssh-client python3 py3-pip zsh curl

sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

cat <<EOP > ~/.profile
alias ll="ls -Al"
alias vim="neovim"
alias vi="neovim"

EOP
